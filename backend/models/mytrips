const mongoose = require('mongoose');

const tripSchema = mongoose.Schema({
    departure :String,
    arrival :String,
    date : Date, 
    price : Number,

});

const MyTrip = mongoose.model('mytrips',tripSchema);
module.exports = MyTrip;